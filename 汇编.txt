movl $1,%rex
CPU产生一个立即数1,并赋值为寄存器rex,立即数前加$,寄存器前加%,目的是和符号名区别

int $0x80
int 指令为软中断指令,可以用这一条指令故意产生一个异常,int $0x80这种形式称为系统调用,系统内核提供了很多系统服务,但是由于用户模式下不能随心所欲操控CPU,因此需要使用int进入系统内核,通过使CPU进入特权模式来进行操作,用户可通过寄存器传递参数,调用系统服务结束后,CPU由特权模式切换回用户模式
